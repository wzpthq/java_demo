/**
 * BIO, NIO，AIO
 *
 * BIO：阻塞IO，也就是会读取数据等待；默认的流、字符就是；
 * NIO：new io，分为两个面，第一个面就是块（buffer）形式的写和读，不再是流的方式；
 *      其次，在网络IO时，出现了线程多路复用的情况。
 * AIO：就是回调策略；
 *
 */

package study.wzp.data.list.part01.lession08;